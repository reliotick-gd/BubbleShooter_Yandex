# BubbleShooter_animals — art overhaul notes

Копия Cozy Animal Town (Unity 6000.4.7f1) для арт-эксперимента: сочный painted-стиль
топ-казуалок (см. промо-референсы). **Логику/вёрстку/UI не меняем — только арт.**
Форк без git (исключены Library/Temp/Build/.git). Прод — соседний проект Cozy Animal Town.

## Решение: PNG, не процедурка
Весь арт рисуется кодом. Потолок процедурки — «стикеры»; референсы это painted-персонажи.
→ Шарики и маскоты = PNG-арт; фон/UI пока процедурные (улучшены).

## PNG-пайплайн шариков (ГОТОВ, drop-in)
`Assets/_Game/Scripts/AnimalSpriteFactory.cs` → `Get(id, body)`:
сначала `Resources.Load<Texture2D>("Animals/" + Names[id])`, спрайт в рантайме
(pivot центр, pixelsPerUnit = ширина текстуры → размер в игре одинаков при любом
разрешении PNG); нет файла → процедурная отрисовка (фолбэк).
`Names` = cat, bear, frog, bunny, fox, chick, panda, dog, rainbow, bomb (id 0-9).
Доска не тинтит (`BoardManager` sr.color = white) → PNG как есть.

## Ассеты `Assets/Resources/Animals/`
- 10 шариков (cat..bomb) — ГОТОВЫ (глянцевый цветной шар + мордочка; цвет = матч).
  Фон вырезан; разрешения разные — ОК. Бэкап: `_art_raw_backup/`.
- 4 маскота: `cat_3x4 / bunny_3x4 / bear_3x4 / panda_3x4` (1024×1536, 3:4, прозрачные)
  — ЕЩЁ НЕ ПОДКЛЮЧЕНЫ. TODO.

## TODO (продолжить здесь)
1. Подключить маскотов на экраны (старт — Победа `Hud.BuildPanel`): Image'ы за box'ом,
   raycast off, не перекрывать кнопки; референс: кот слева-низ, зайка+мишка справа-низ.
   Фолбэк: нет файла → ничего. Лучше вынести маскотов в `Resources/Mascots/`
   (в Resources/Animals они зря раздувают билд).
2. Опц.: сочный «поп» при лопании (конфетти+звёзды), сцена города/титул с маскотами.

## Улучшенная процедурка (остаётся для фона/UI/иконок)
- `AnimalSpriteFactory.Sphere()` — объёмный шар + двойной глянец.
- `GameConfig.palette` — карамельные 8 цветов.
- `CozyBackground` — лазурное небо, солнце, сочные холмы, искорки, 46 цветов на лужке.
- `UiKit.BuildRounded` — вертикальный градиент объёма + sheen на кнопках.
- `UiSymbols` — ★/☆/♥ глянцево-градиентные.
